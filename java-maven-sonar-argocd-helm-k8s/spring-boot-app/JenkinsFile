pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                // Checkout the repository
                git 'https://github.com/Amneet10/Jenkins-Zero-To-Hero.git'
            }
        }
        // Add more stages as needed for your pipeline
    }
}
